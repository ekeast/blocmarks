<h1>Topics</h1>
<h3> <%= link_to "Add a New Topic", new_topic_path, method: :get %> </h3>

<% @topics.each do |topic| %>
  <%= topic.title %> | <%= link_to "Delete Topic", topic_path(topic), method: :delete %></br>
<% end %>
